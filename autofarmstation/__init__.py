"""AutoFarmStation - 多开挂机大师."""

__version__ = "1.1.0"
__app_name__ = "AutoFarmStation"
__app_name_cn__ = "多开挂机大师"
__author_handle__ = "zlwzk"

# === 项目简介(同时用于 GitHub repo 描述、README 顶部、Release 标题) ===
PROJECT_TAGLINE = (
    "一款专为挂机游戏设计的 Windows 多开自动化工作站。"
    "实时预览、连点器、键盘宏、宏录制、多窗口同步执行、Steam 挂机游戏预设,一站搞定。"
)
PROJECT_DESCRIPTION = (
    "AutoFarmStation(多开挂机大师)是一款面向 Steam 挂机游戏 / 放置类游戏 / 放置网页游戏的"
    "Windows 多开自动化工具。\n"
    "\n"
    "• 一屏掌控所有挂机窗口:左侧自适应预览网格,右侧统一操作面板\n"
    "• 内置连点器、键盘宏、宏录制/回放\n"
    "• **多窗口同步 / 单窗口独立**:勾选任意几个窗口,一键让它们同步执行同一套连贯操作;\n"
    "  每个窗口又各自保存一份独立配置,可以分开执行互不相同的操作\n"
    "• 自带 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、"
    "Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity 等),一键应用\n"
    "• 支持任意自定义进程(不仅限 Steam),所有路径/用户名/游戏目录自动脱敏\n"
    "• 进程监控、定时调度、运行时长统计,7×24 托管你的挂机农场\n"
    "\n"
    "完全本地运行,无需联网,无广告无追踪。"
)

# === Release 公告(也作为 GitHub Release 正文) ===
RELEASE_NOTES = """\
# AutoFarmStation v1.1.0 · 多窗口同步执行

新增「多窗口同步 / 单窗口独立」双模执行,这次终于可以放心多开了。

## 本版新增

### 1. 多窗口同步执行(核心新功能)

左侧预览网格的每张卡片右上角都多了一个**勾选框**。勾选任意几个窗口后:

| 按钮 | 作用 |
|------|------|
| 📡 **应用当前配置** | 把右侧面板正在编辑的那套操作(点位 / 按键序列)复制到所有勾选窗口 |
| ▶ **同步启动** | 对勾选窗口同时启动(用各窗口自己已保存的配置) |
| ■ **同步停止** | 一键停掉勾选窗口 |

在右侧「连点器」「键盘宏」面板里也各有一组 **📡 同步启动(勾选窗口)** / **■ 同步停止** 按钮 ——
点一下就把当前这套操作广播到所有勾选窗口并同时开跑。

### 2. 单窗口仍然完全独立

广播只是「复制一份初始配置」,之后**每个窗口各存一份**:

- 窗口 A 可以挂着「左下角连续点 5 次」,
- 窗口 B 同时挂着「右下角连续点 3 次」,
- 窗口 C 挂着完全不同的键盘宏 —— 三者互不干扰。

想只操作某一个窗口?直接点它卡片上的「连点」按钮,或在右侧面板选中它后点「启动」。

### 3. 其它改进

- **状态栏实时汇总**:显示「追踪 N · 运行 N · 已选 N」,一眼看清整体挂机状态。
- **预览卡片状态回填**:卡片底栏实时显示该窗口的运行时长与累计点击次数。
- **配置持久化**:每个窗口的连点器点位 / 键盘宏序列会随窗口一起保存,
  重启软件后自动恢复(失效窗口的配置会自动清理)。
- **状态栏统一中枢**:单窗口启停、多窗口同步、全局热键(F9/F10/急停)现在共用同一套
  自动化中枢,不会再出现「面板显示已停止但实际还在跑」的情况。
- **自检扩充到 19 项**:新增自动化中枢(单窗口独立 + 多窗口同步 + 序列化)与
  界面离屏装配两项自检。

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选则只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |

## 完整功能

- **多窗口预览**:左侧自适应网格,实时刷新,点击聚焦,双击最大化。
- **连点器**:多点击点位轮询、随机抖动、左右中/双击支持、可视化选点。
- **键盘宏**:按键序列回放、持续按键、单键循环。
- **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON。
- **多窗口同步 / 单窗口独立**:见上文。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **定时任务**:定时启停动作、每日定时重启游戏。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10+ 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、
  Clicker Heroes、Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、
  AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:全局热键、截图帧率、深色/浅色主题、开机自启动、检查更新。

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\\AutoFarmStation\\`。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\\...` / `%USERPROFILE%\\...` 占位符展示。

## 反馈

应用内「帮助 → 反馈建议」一键上传(已脱敏)到 GitHub Issues。
仓库:https://github.com/zlwzk/AutoFarmStation
"""