"""AutoFarmStation - 多开挂机大师."""

__version__ = "1.4.1"
__app_name__ = "AutoFarmStation"
__app_name_cn__ = "多开挂机大师"
__author_handle__ = "zlwzk"

# === 项目简介(同时用于 GitHub repo 描述、README 顶部、Release 标题) ===
PROJECT_TAGLINE = (
    "一款专为挂机游戏设计的 Windows 多开自动化工作站。"
    "窗口嵌入、实时预览、连点器、键盘宏、宏录制、多窗口同步执行、Steam 状态联动、"
    "Steam 挂机游戏预设,一站搞定。"
)
PROJECT_DESCRIPTION = (
    "AutoFarmStation(多开挂机大师)是一款面向 Steam 挂机游戏 / 放置类游戏 / 放置网页游戏的"
    "Windows 多开自动化工具。\n"
    "\n"
    "• **窗口嵌入**:把游戏窗口收进软件里,桌面上不再散落一堆窗口,一屏掌控所有挂机农场\n"
    "• 左侧自适应预览网格实时显示每个窗口画面(GDI 抓帧,被遮挡也能截到)\n"
    "• 内置连点器、键盘宏、宏录制/回放\n"
    "• **多窗口同步 / 单窗口独立**:勾选任意几个窗口,一键让它们同步执行同一套连贯操作;\n"
    "  每个窗口又各自保存一份独立配置,可以分开执行互不相同的操作\n"
    "• **Steam 状态联动**:开始挂机自动把 Steam 状态切到「在线 / 离开 / 隐身」,\n"
    "  挂机停完自动还原(本地实现,不碰账号密码)\n"
    "• 自带 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、"
    "Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity 等),一键应用\n"
    "• 支持任意自定义进程(不仅限 Steam),所有路径/用户名/游戏目录自动脱敏\n"
    "• 进程监控、定时调度、运行时长统计,7×24 托管你的挂机农场\n"
    "\n"
    "完全本地运行,无需联网,无广告无追踪。"
)

# === Release 公告(也作为 GitHub Release 正文) ===
RELEASE_NOTES = """\
# AutoFarmStation v1.4.1 · 自动检查更新(频率可调 / 横幅提示 / 跳过版本)

每次打开软件都会提示:要不要把上次关闭时追踪的窗口加回来?

## 本版新增

### 1. 自动检查更新(设置 → 更新与日志)

之前默认是固定 1 小时一次的硬编码,设置里只有一个勾选框。
这一版做完整:

| 项 | 说明 |
|----|------|
| **开关** | 「启动时自动检查更新(后台静默,不打扰)」 |
| **频率** | 1 / 6 / 12 / 24 小时 / 每周 —— 默认每小时 |
| **上次检查** | 显示 `2026-09-15 12:34:56 · 2 小时前 · 发现 v1.5.0` |
| **立即检查** | 一键同步最新状态并弹框 |
| **跳过版本** | 弹框里加「忽略」按钮,选过的版本启动时不再提示 |

**提示方式分两类**:
- 启动 3 秒后 / 每 N 小时 自动跑 → **不弹框**,只在窗口顶部贴一个非模态横幅:
  > 「发现新版本 v1.5.0(当前 v1.4.1)  · [去下载] [✕]」
  > 不打扰,但你想点就点。
- 「立即检查」/菜单 → 工具 → 检查更新 → **弹模态框**,带更新日志摘要。

「忽略」按钮 = 跳过当前版本不再弹横幅;点「去下载」或装新版本自动清掉。

### 2. v1.4.0 回顾(同包保留)

- **Bat 脚本库**(工具 → Bat 脚本库...,Ctrl+B):4 个示例脚本,运行不弹黑窗,支持参数
- **禁用 Steam 叠加层**(Shift+Tab):写 steam.cfg + 备份/还原
- **设置界面扩充**:界面/预览/默认连点参数/Steam/更新与日志 共 5 组
- 启动时最小化(到任务栏)真生效

## 本版新增

### 1. Bat 脚本库(工具 → Bat 脚本库...)

把常用的「快捷加 mod / 改设置 / 清理缓存」一类 .bat 收进软件里,
一键运行,**不弹黑色 cmd 窗口**。

- 仓库内置 4 个示例(`bat/` 目录):
  - **禁用 Steam 叠加层**(Shift+Tab 不再唤起)
  - **启用 Steam 叠加层**(默认行为)
  - **清空 Steam 下载缓存**(修下载卡 0%)
  - **[模板] 一键安装 mod**(自带参数解析,按需改命令)
- 用户目录:`%APPDATA%\AutoFarmStation\bats\`,首次启动自动把内置拷一份过来,自由修改
- 「运行」前可填参数(manifest 里声明),支持 `--key=value` 形式传给 bat
- 「编辑脚本」用系统默认编辑器打开;内置脚本不能直接改,自动复制到用户目录后再编辑
- 新建/删除用户脚本;`explorer /select,` 高亮文件
- 全部用 `subprocess.Popen(..., creationflags=CREATE_NO_WINDOW)`,后台跑、无黑窗

### 2. 禁用 Steam 叠加页快捷键(设置 → Steam)

勾选「禁用 Steam 叠加层(Shift+Tab 不再唤起)」后:

- 工具把 `%APPDATA%\Steam\steam.cfg` 的 `[Install] SteamOverlay` 改成 `0`
- Steam store 进程下次启动读取,新启动的游戏不再注入 `GameOverlayRenderer.dll`
- **运行中的游戏不会受影响** —— 想让 Shift+Tab 在挂机时不再弹叠加层,先勾选 → 关 Steam 重启 → 开游戏
- 「还原」按钮 = 把写入前的 steam.cfg 内容回滚,回到开启状态
- 同时显示当前 steam.cfg 里 SteamOverlay 的实际值,看一眼就知道有没有生效

### 3. 设置界面扩充(原来只有 1 组,现在 5 组)

| 组 | 新增项 |
|----|--------|
| 界面 | 主题 · 语言 · 启动时最小化(到任务栏)· 退出确认 · 启动恢复窗口 |
| 预览 | 刷新频率 · 默认列数 |
| 默认连点参数 | 间隔 / ±抖动 / 按键 / 模式(影响新建窗口的初始值)|
| Steam 状态联动 & 叠加层 | 在线状态切换 + **禁用叠加层**(上面那条)|
| 更新与日志 | 自动检查更新 · 日志级别 |

数据位置多了一行 `Bat 脚本库:%APPDATA%\AutoFarmStation\bats\`。

### 4. v1.3.0 → v1.3.1(同包保留)

- **启动恢复上次窗口**:匹配规则按 exe basename + 标题前缀做 4 级评分,贪心分配避免重复命中
- 自检 22 项 → **本版 24 项**:新增 `bat_library`(合并/创建/删除/损坏恢复/seed)、`steam_overlay`(describe/写/还原/重复写不重复)
- 版本号 1.3.1 → **1.4.0**(新增 Bat 库 + Steam 叠加层 + 设置扩充,有功能性变化)

### 2. v1.3.0 改进(同版本随包)

- **窗口嵌入**:每张预览卡片的标题栏多了一个「嵌入」按钮 —— 原窗口被收进卡片内显示,
  桌面和任务栏上不再单独出现;关闭/退出自动还原。
- **预览「无图像」修复**:改用 GDI `PrintWindow` 直接抓取窗口客户区,被遮挡也能截到。
- **多开优化**:截图错峰 + 失败退避,多开十几个窗口也不空转烧 CPU。

### 3. 其它

- **自检 22 项全过**:新增「session」自检 —— 保存/读取/匹配/前缀提取/贪心分配/损坏恢复。
- 版本号 1.3.0 → **1.3.1**(纯新增,无破坏性改动)。

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选时只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |

## 完整功能

- **窗口嵌入**:把游戏窗口收进软件内,一键嵌入/弹出,退出自动还原。
- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),GDI 实时抓帧,点击聚焦,双击最大化。
- **多窗口同步 / 单窗口独立**:勾选多个窗口一键同步执行同一套操作;每个窗口又可各自独立配置。
  - **连点器**:多点击点位轮询、可设随机抖动、左右中/双击支持、可视化选点。
  - **键盘宏**:按键序列回放、持续按键、单键循环,任意组合。
  - **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **Steam 状态联动**:挂机自动切状态、停完自动还原(在线 / 离开 / 隐身)。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **定时任务**:定时启停某个动作、每日定时重启游戏。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10+ 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、
  Clicker Heroes、Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、
  AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:全局热键、Steam 状态联动、截图帧率、深色/浅色主题、开机自启动、检查更新。

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\\AutoFarmStation\\`。
- 窗口嵌入只调用 Windows 自身的 `SetWindowPos`/`SetParent` 窗口接口,不注入、不读写目标进程内存。
- Steam 状态联动只写 Steam 自己的 `steam://` 协议 + 读本地 `localconfig.vdf`,
  **不联网、不读账号密码、不碰 Steam 令牌文件**。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\\...` / `%USERPROFILE%\\...` 占位符展示。

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues(已脱敏)。
仓库:https://github.com/zlwzk/AutoFarmStation
"""
