# AutoFarmStation v1.6.5 · Bat 脚本库:同时支持「拖拽进来」与「点按钮选文件」

v1.6.4 加了「拖拽 .bat 进来」,但部分环境(某些 Qt 版本 / Windows 焦点状态)光标会显示「禁止」、根本收不到 drop;
v1.6.5 直接给两个入口 —— **点选 + 拖拽双保险**,还把拖放做得更稳。

---

## 这次加了 / 改了什么

| 改动 | 之前 | 现在 |
|------|------|------|
| **「📁 从文件选择 .bat / .cmd…」按钮** | 没有 | 顶部多一个按钮 → 弹 Windows 文件选择框 → 可多选 → 自动导入。**不想拖的也能用** |
| **拖拽更稳:三层 acceptDrops** | 只在 QDialog 启用 | dialog / 树(viewport) / 右侧详情 三层都启用,绕过某些 Qt 版本 QSplitter / QTreeWidget 拦截事件的问题 |
| **拖拽视觉反馈** | 无 | 拖入有效文件时提示框变成蓝色高亮「📥 松开鼠标即可导入 N 个脚本」;拖出窗口自动还原 |
| **共享 `_import_paths()`** | dropEvent 自己循环调 import_bat | 拖拽 / 点选共用 `_import_paths(paths, source="拖拽"\|"选择")` —— 一处逻辑、两条入口 |
| **修正类型注解** | `dragMoveEvent(ev: QDragEnterEvent)` | `dragMoveEvent(ev: QDragMoveEvent)` + `dragLeaveEvent` 显式还原 |
| **`_is_bat_url` 矩阵** | 只在 dragEnter 用 | 自检覆盖 .bat/.cmd/.txt/.exe/远程 URL/无后缀六种 case |

修法要点:

- `ui/bat_library_dialog.py`:
  - 顶部新增 `QPushButton("📁 从文件选择 .bat / .cmd…")` → `_on_pick_files()` → `QFileDialog.getOpenFileNames(..., "批处理文件 (*.bat *.cmd);;所有文件 (*.*)")` → `_import_paths(paths, source="选择")`
  - `_tree.setAcceptDrops(True)` + `_tree.setDragDropMode(DropOnly)` + `_tree.viewport().setAcceptDrops(True)` —— 树自己接 drop
  - `right` QWidget 也 `setAcceptDrops(True)`,所以拖到右侧空白区也能放
  - 提示文字改成 `📥 两种方式快速加脚本:① 把 .bat / .cmd 从文件管理器拖到本窗口任一处  ② 点上方「📁 从文件选择 .bat / .cmd…」`
  - 拖入时 `_set_hint_active(True, "📥 松开鼠标即可导入 N 个脚本")` 改样式表 + 文字;`dragLeaveEvent` 时还原

- 行为兼容:v1.6.4 已拖过的用户不受任何影响 —— 新版只是多给一个入口,manifest / 用户目录 / 已有的脚本全部不动。

---

## 安全 + 不破坏老用户

- 用户目录的旧 manifest / 旧 .bat 全部不动 —— 「拖拽 / 选择」只是**新增**条目,不会触发内置 seed,
  也不会读 / 写任何内置脚本。
- 拖进来的脚本运行时**仍走原 subprocess 通道**(非模态后台运行,不弹黑色 cmd 窗口),行为与
  你用「新建脚本」建的完全一致。
- 顶部会贴一行「来源:xxx.bat / 导入于:2026-09-15 21:43」注释,日后想撤回 / 知道从哪来的,
  一眼可见。

---

## 数据本地 + 用户隔离 + 升级不丢(再次强调)

- 所有数据严格存在 `%APPDATA%\AutoFarmStation\`,与你的 Windows 账户绑定。
- 升级版本不会清空配置;新字段全部带默认值(老 `config.json` 直接能用)。
- **绝对不上传**:窗口标题、进程路径、用户名、机器名、AppData、TEMP 路径
  都不出现在仓库 commit、日志、issue、反馈文本里。

---

## 顺带

- 「新建脚本」按钮**未删**,保留给「纯新建 / 还不存在 bat 文件」的场景。
- 老的「立刻打开文件管理器定位 / 编辑脚本 / 运行 / 删除」按钮全部不变。
- 自检 **46 项** 全过(v1.6.4 是 44 项;新增 2 项:`_is_bat_url` 矩阵、`_on_pick_files` → `_import_paths` 透传)。
- 「立即更新」(v1.6.3) 按钮 + 检查更新定时器 + 启动横幅 + 「立即检查 → 前往下载」全部不变。

---

## 兼容与升级

- 老版本升上来:直接覆盖 exe,配置不动,Bat 库对话框立刻出现「拖拽」提示并接受拖放 + 「📁 从文件选择」按钮。
- 已有的内置 / 用户脚本**不会被此次升级改名 / 移位 / 改 category**。

---

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选时只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |
| Ctrl + T | 定时任务 |
| Ctrl + B | Bat 脚本库 |
| Ctrl + , | 设置 |

---

## 完整功能

- **窗口嵌入**:把游戏窗口收进软件内,一键嵌入/弹出,退出自动还原。
- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),GDI 实时抓帧,点击聚焦(铺满工作区),双击最大化。
- **每窗口独立边距 + 画面缩放**:按窗口设置聚焦时的上 / 下 / 左 / 右像素边距,以及画面缩放比例
  (边框全屏游戏会跟着等比缩放)。
- **多窗口同步 / 单窗口独立**:勾选多个窗口一键同步执行同一套操作;每个窗口又可各自独立配置。
  - **连点器**:多点击点位轮询、随机抖动、左右中/双击支持、可视化选点、比例坐标抗缩放。
    - **点击方式 = 自动**(默认):先后台投递、失败自动改真实输入;可锁定成「只投递」/「只真实输入」。
  - **键盘宏**:按键序列回放、持续按键、单键循环,任意组合,按窗口尺寸等比映射。
  - **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **进程管理**:卡片停止/恢复进程,恢复时自动唤醒游戏(Steam 游戏先启动 Steam),退出软件可同步关闭游戏。
- **进程识别更稳**:
  - 「选择窗口」可显示隐藏 / 无标题的窗口;
  - 工具菜单「按 PID 添加」/「按进程名添加」,连 PID 直接补齐遗漏的进程。
- **音量控制**:整体音量只作用于已加入窗口(不动系统总音量),单窗口会话音量,可静音、可还原快照。
- **定时任务**:一次性 / 间隔 / 每天 / 每周(可多选) 四种频率 + 延时执行,十种动作,持久化保存。
- **挂机时段**:只在指定时间段挂机,时段外自动暂停 / 停止,回到时段给出提示。
- **资源监控**:卡片显示 CPU / 内存占用,超阈值提醒一次。
- **日志查看器**:界面内过滤 / 复制 / 导出日志。
- **配置备份**:设置 / 窗口 / 预设 / 宏 / 任务打包导出,导入前自动备份。
- **Steam 状态联动**:挂机自动切状态、停完自动还原(在线 / 离开 / 隐身)。
- **Steam 叠加层控制**:一键关闭 Shift+Tab,写 steam.cfg + 备份还原。
- **Bat 脚本库**:内置 + 用户脚本两路合并,运行不弹黑窗,支持参数,**跨更新不丢用户改过的脚本**;
  **v1.6.4 起支持拖拽 .bat / .cmd 快速导入,自动识别编码,同名自动后缀**;
  **v1.6.5 起再点选按钮也能批量导入,拖拽更稳(三层 acceptDrops + 视觉反馈)**。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、
  Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:界面、预览、默认连点参数、游戏进程、挂机时段、资源监控、音量、Steam、更新与日志。
- **DPI 感知**:启动时声明 Per-Monitor DPI Aware,聚焦 / SendInput / 嵌入都与系统 DPI 缩放吻合。
- **一键更新**:**设置 → 立即更新**,下载 + 校验 + 替换 + 重启全自动,不再弹浏览器。

---

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\AutoFarmStation\`。
- 窗口嵌入只调用 Windows 自身的 `SetWindowPos`/`SetParent` 窗口接口,不注入、不读写目标进程内存。
- 音量控制只调用 Windows Core Audio 接口,不录屏、不采集音频数据。
- 自动唤醒只调用 `subprocess` / `steam://` 协议,不注入游戏、不改游戏文件。
- Steam 状态联动只触发 Steam 自己的 `steam://` 协议、只读本地 `localconfig.vdf`:
  **不联网、不读账号密码、不碰 Steam 令牌文件**。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符展示。
- 不上传任何数据,更新检查只读取 GitHub Releases。
- **自动更新走的也是 GitHub Releases 公开 API**,只下 `AutoFarmStation.exe` 一个文件,
  不传任何机器信息(User-Agent 只有「AutoFarmStation/<版本> (Windows)」三段)。

---

## 自检

`python -m scripts.selftest` **46 项全过**(v1.6.4 是 44 项)。

---

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues(已脱敏)。
仓库:[github.com/zlwzk/AutoFarmStation](https://github.com/zlwzk/AutoFarmStation)

---

## 文件说明

- `AutoFarmStation.exe` — 主程序,单文件 exe,Windows 10/11 64 位

---

## 历史版本(已发布)

<details>
<summary><b>v1.6.4 · Bat 脚本库支持拖拽 .bat / .cmd 快速添加</b>(点击展开)</summary>

v1.6.3 加了「设置一键更新」;v1.6.4 顺手把 **Bat 脚本库对话框**的入口打得更顺——
**直接把文件管理器里的 .bat / .cmd 拖进对话框,自动加为脚本**,再也不用
「新建脚本 → 编辑器打开 → 复制内容回去」三步走。

| 改动 | 之前 | 现在 |
|------|------|------|
| **Bat 库拖拽导入** | 只能「新建脚本」起名,再用编辑器粘代码 | 从文件管理器拖 .bat / .cmd 进对话框 → **自动落到用户目录、自动写 manifest、自动选中** |
| **自动识别编码** | 新建脚本只给空模板 | 拖进来时**自动识别 utf-8 / GBK / utf-16**(中文 Windows 批处理常用 GBK),中文不乱码 |
| **同名不覆盖** | 不存在 | 同名拖第二次自动加 `_2` / `_3` ... 后缀,**绝不覆盖你已有的脚本** |
| **拒绝非 bat 文件** | 不存在 | 拖 `.txt` / `.exe` 直接拒收,光标显示「禁止」,状态栏给出明确反馈 |

修法要点:

- `core/bat_library.py::BatLibrary.import_bat(src_path, *, category, title, desc)`:
  - 校验源是真实文件且后缀是 `.bat` / `.cmd`(否则 `ValueError`)
  - 读源字节,逐个尝试 `utf-8-sig` → `utf-8` → `gbk` → `utf-16` 解码,失败再回退 `latin-1`
  - 目标文件名 = 安全化的源 stem + `.bat`;已存在则自动 `_2` / `_3` ...(扫到 1000 个为止)
  - 在内容前加 4 行 `rem` 注释(来源 / 导入时间 / 目标路径),方便你日后追溯
  - 写一条 `category="我的脚本"` + `tags=["拖拽导入"]` 的 manifest 条目,id 形如 `user_<safe>_<hex6>`
- `ui/bat_library_dialog.py`:
  - `setAcceptDrops(True)` 在对话框层级启用拖放
  - `dragEnterEvent` / `dragMoveEvent` 仅在拖的是本地 `.bat` / `.cmd` URL 时接受,其它一律拒绝(光标显示禁止)
  - `dropEvent` 逐个调用 `import_bat`,完成后给一条汇总状态:
    「已导入 N 个:xxx、yyy、...  |  失败 M 个:• 文件名:原因」
  - 顶部加了一行小灰字提示:`📥 提示:可从文件管理器把 .bat / .cmd 拖到本窗口,快速加为脚本`

</details>

<details>
<summary><b>v1.6.3 · 设置里直接「一键更新」,不弹浏览器不拷链接</b>(点击展开)</summary>

v1.6.2 修了一批「假设置」;v1.6.3 只加一个真正省事的特性 —— **设置 → 更新与日志
里点「立即更新」就能下载新版 exe、自动替换、自动重启新版本**。

| 改动 | 之前 | 现在 |
|------|------|------|
| 「立即更新」按钮 | 设置里只有「立即检查」→ 弹窗「是 / 否」→ 还得自己开浏览器 | 点按钮直接下新版 exe、覆盖、启动新版本,**不再弹浏览器** |
| 进度条 + 取消 | 完全没可视化 | 模态进度条,可中途取消(自动清理半截文件) |
| 下载失败兜底 | 任何网络问题都直接断 | 弹窗明确告诉你失败原因,主程序不退出 |

实现:`utils/updater.py` 流式下载 + PowerShell 交换助手
(`Wait-Process -Timeout 30` 等父进程退出 → `Copy-Item -Force` 覆盖 →
`Start-Process` 启动新版本 → 删 `%TEMP%\AutoFarmStation_<v>.exe`);
`main_window.closeEvent` 检测到 `cfg.settings.pending_update_path` 时跳过
确认弹窗 + 跳过杀游戏进程。

</details>

---

## 安全 + 不破坏老用户

- 用户目录的旧 manifest / 旧 .bat 全部不动 —— 「拖拽导入」只是**新增**条目,不会触发内置 seed,
  也不会读 / 写任何内置脚本。
- 拖进来的脚本运行时**仍走原 subprocess 通道**(非模态后台运行,不弹黑色 cmd 窗口),行为与
  你用「新建脚本」建的完全一致。
- 顶部会贴一行「来源:xxx.bat / 导入于:2026-09-15 21:43」注释,日后想撤回 / 知道从哪来的,
  一眼可见。

---

## 数据本地 + 用户隔离 + 升级不丢(再次强调)

- 所有数据严格存在 `%APPDATA%\AutoFarmStation\`,与你的 Windows 账户绑定。
- 升级版本不会清空配置;新字段全部带默认值(老 `config.json` 直接能用)。
- **绝对不上传**:窗口标题、进程路径、用户名、机器名、AppData、TEMP 路径
  都不出现在仓库 commit、日志、issue、反馈文本里。

---

## 顺带

- 「新建脚本」按钮**未删**,保留给「纯新建 / 还不存在 bat 文件」的场景。
- 老的「立刻打开文件管理器定位 / 编辑脚本 / 运行 / 删除」按钮全部不变。
- 自检 **44 项** 全过(v1.6.3 是 40 项;新增 4 项:基本导入含 GBK 中文、重名后缀、拒非 bat、拒不存在路径)。
- 「立即更新」(v1.6.3) 按钮 + 检查更新定时器 + 启动横幅 + 「立即检查 → 前往下载」全部不变。

---

## 兼容与升级

- 老版本升上来:直接覆盖 exe,配置不动,Bat 库对话框立刻出现「拖拽」提示并接受拖放。
- 已有的内置 / 用户脚本**不会被此次升级改名 / 移位 / 改 category**。

---

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选时只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |
| Ctrl + T | 定时任务 |
| Ctrl + B | Bat 脚本库 |
| Ctrl + , | 设置 |

---

## 完整功能

- **窗口嵌入**:把游戏窗口收进软件内,一键嵌入/弹出,退出自动还原。
- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),GDI 实时抓帧,点击聚焦(铺满工作区),双击最大化。
- **每窗口独立边距 + 画面缩放**:按窗口设置聚焦时的上 / 下 / 左 / 右像素边距,以及画面缩放比例
  (边框全屏游戏会跟着等比缩放)。
- **多窗口同步 / 单窗口独立**:勾选多个窗口一键同步执行同一套操作;每个窗口又可各自独立配置。
  - **连点器**:多点击点位轮询、随机抖动、左右中/双击支持、可视化选点、比例坐标抗缩放。
    - **点击方式 = 自动**(默认):先后台投递、失败自动改真实输入;可锁定成「只投递」/「只真实输入」。
  - **键盘宏**:按键序列回放、持续按键、单键循环,任意组合,按窗口尺寸等比映射。
  - **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **进程管理**:卡片停止/恢复进程,恢复时自动唤醒游戏(Steam 游戏先启动 Steam),退出软件可同步关闭游戏。
- **进程识别更稳**:
  - 「选择窗口」可显示隐藏 / 无标题的窗口;
  - 工具菜单「按 PID 添加」/「按进程名添加」,连 PID 直接补齐遗漏的进程。
- **音量控制**:整体音量只作用于已加入窗口(不动系统总音量),单窗口会话音量,可静音、可还原快照。
- **定时任务**:一次性 / 间隔 / 每天 / 每周(可多选) 四种频率 + 延时执行,十种动作,持久化保存。
- **挂机时段**:只在指定时间段挂机,时段外自动暂停 / 停止,回到时段给出提示。
- **资源监控**:卡片显示 CPU / 内存占用,超阈值提醒一次。
- **日志查看器**:界面内过滤 / 复制 / 导出日志。
- **配置备份**:设置 / 窗口 / 预设 / 宏 / 任务打包导出,导入前自动备份。
- **Steam 状态联动**:挂机自动切状态、停完自动还原(在线 / 离开 / 隐身)。
- **Steam 叠加层控制**:一键关闭 Shift+Tab,写 steam.cfg + 备份还原。
- **Bat 脚本库**:内置 + 用户脚本两路合并,运行不弹黑窗,支持参数,**跨更新不丢用户改过的脚本**;
  **v1.6.4 起支持拖拽 .bat / .cmd 快速导入,自动识别编码,同名自动后缀**。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、
  Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:界面、预览、默认连点参数、游戏进程、挂机时段、资源监控、音量、Steam、更新与日志。
- **DPI 感知**:启动时声明 Per-Monitor DPI Aware,聚焦 / SendInput / 嵌入都与系统 DPI 缩放吻合。
- **一键更新**:**设置 → 立即更新**,下载 + 校验 + 替换 + 重启全自动,不再弹浏览器。

---

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\AutoFarmStation\`。
- 窗口嵌入只调用 Windows 自身的 `SetWindowPos`/`SetParent` 窗口接口,不注入、不读写目标进程内存。
- 音量控制只调用 Windows Core Audio 接口,不录屏、不采集音频数据。
- 自动唤醒只调用 `subprocess` / `steam://` 协议,不注入游戏、不改游戏文件。
- Steam 状态联动只触发 Steam 自己的 `steam://` 协议、只读本地 `localconfig.vdf`:
  **不联网、不读账号密码、不碰 Steam 令牌文件**。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符展示。
- 不上传任何数据,更新检查只读取 GitHub Releases。
- **自动更新走的也是 GitHub Releases 公开 API**,只下 `AutoFarmStation.exe` 一个文件,
  不传任何机器信息(User-Agent 只有「AutoFarmStation/<版本> (Windows)」三段)。

---

## 自检

`python -m scripts.selftest` **44 项全过**(v1.6.3 是 40 项)。

---

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues(已脱敏)。
仓库:[github.com/zlwzk/AutoFarmStation](https://github.com/zlwzk/AutoFarmStation)

---

## 文件说明

- `AutoFarmStation.exe` — 主程序,单文件 exe,Windows 10/11 64 位

---

## 历史版本(已发布)

<details>
<summary><b>v1.6.3 · 设置里直接「一键更新」,不弹浏览器不拷链接</b>(点击展开)</summary>

v1.6.2 修了一批「假设置」;v1.6.3 只加一个真正省事的特性 —— **设置 → 更新与日志
里点「立即更新」就能下载新版 exe、自动替换、自动重启新版本**。

| 改动 | 之前 | 现在 |
|------|------|------|
| 「立即更新」按钮 | 设置里只有「立即检查」→ 弹窗「是 / 否」→ 还得自己开浏览器 | 点按钮直接下新版 exe、覆盖、启动新版本,**不再弹浏览器** |
| 进度条 + 取消 | 完全没可视化 | 模态进度条,可中途取消(自动清理半截文件) |
| 下载失败兜底 | 任何网络问题都直接断 | 弹窗明确告诉你失败原因,主程序不退出 |

实现:`utils/updater.py` 流式下载 + PowerShell 交换助手
(`Wait-Process -Timeout 30` 等父进程退出 → `Copy-Item -Force` 覆盖 →
`Start-Process` 启动新版本 → 删 `%TEMP%\AutoFarmStation_<v>.exe`);
`main_window.closeEvent` 检测到 `cfg.settings.pending_update_path` 时跳过
确认弹窗 + 跳过杀游戏进程。

</details>

<details>
<summary><b>v1.6.2 · 修一批「改了设置」实际不生效的老 bug</b>(点击展开)</summary>

v1.6.1 解决了「点击、聚焦、缩放、识别」四件大事;但社区反馈:设置面板里
**6 项勾上 / 改完根本没生效** —— 这一版按根因逐条修。

| # | 设置项 | 修法 |
|---|--------|------|
| 1 | 主题(深 / 浅色) | `app._apply_palette(theme)` 真的读 `cfg.ui.theme`,立刻应用调色板 |
| 2 | 语言(English) | 占位项**直接禁用**,鼠标移上去注明「规划中」,避免误选 |
| 3 | 默认连点参数 | `ClickerPanel` 构造时读 `defaults.clicker_*`,新建连点器真的用上 |
| 4 | 启动时最小化 | `MainWindow` 启动后注册 `showEvent`,第一次显示时按 `ui.start_minimized` 最小化 |
| 5 | 挂机时段 | 新增 `FarmWindowGuard` + 30 秒 `QTimer`,时段外拒绝启动 + 自动停,回到时段给提示 |
| 6 | Steam 叠加层「禁用」 | 设置面板**确认**时自动写一次 steam.cfg + 备份,不再要求手动点「立即写入」 |
| 7 | Bat 脚本库每次更新都丢 | `ensure_seeded` 改为「**只追加不覆盖**」:内置 .bat 缺失自动恢复;新条目合并进用户 manifest,用户改过的 title / desc / args 永远保留 |

</details>