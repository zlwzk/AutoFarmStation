# AutoFarmStation v1.4.1 · 自动检查更新(频率可调 / 横幅提示 / 跳过版本)

之前默认是固定 1 小时一次的硬编码,设置里只有一个勾选框。
这一版做完整。

---

## 本版新增

### 1. 自动检查更新(设置 → 更新与日志)

| 项 | 说明 |
|----|------|
| **开关** | 「启动时自动检查更新(后台静默,不打扰)」 |
| **频率** | 1 / 6 / 12 / 24 小时 / 每周 —— 默认每小时 |
| **上次检查** | 显示 `2026-09-15 12:34:56 · 2 小时前 · 发现 v1.5.0` |
| **立即检查** | 一键同步最新状态并弹框 |
| **跳过版本** | 弹框里加「忽略」按钮,选过的版本启动时不再提示 |

**提示方式分两类**:
- 启动 3 秒后 / 每 N 小时 自动跑 → **不弹框**,只在窗口顶部贴一个非模态横幅:
  > 「发现新版本 v1.5.0(当前 v1.4.1)  · [去下载] [✕]」
  > 不打扰,但你想点就点。
- 「立即检查」/菜单 → 工具 → 检查更新 → **弹模态框**,带更新日志摘要。

「忽略」按钮 = 跳过当前版本不再弹横幅;点「去下载」或装新版本自动清掉。

### 2. v1.4.0 回顾(本包里就一并打了)

- **Bat 脚本库**(工具 → Bat 脚本库...,Ctrl+B):4 个示例脚本,运行不弹黑窗,支持参数
- **禁用 Steam 叠加层**(Shift+Tab):写 steam.cfg + 备份/还原
- **设置界面扩充**:界面/预览/默认连点参数/Steam/更新与日志 共 5 组
- 启动时最小化(到任务栏)真生效

---

## v1.4.0 → v1.3.0 的中间变化一并保留

### v1.4.0 总结

- **Bat 脚本库**:仓库根 `bat/manifest.json` + `%APPDATA%\AutoFarmStation\bats\` 用户目录合并,4 个内置示例(禁用/启用 Steam 叠加层、清缓存、mod 安装模板)
- **禁用 Steam 叠加层** Shift+Tab:写 `%APPDATA%\Steam\steam.cfg` 加 `SettingStore SteamOverlayGameLaunchEnable 0`,原文件全量备份到 `settings.steam_overlay_backup`,关设置时一键还原
- **设置界面**:5 组(界面/预览/默认连点参数/Steam/更新与日志)
- **启动时最小化** 真的生效(以前只有最小化到托盘)

### v1.3.0 → v1.2.0 关键变化

- **窗口嵌入**(核心新功能):点卡片标题栏「嵌入」把游戏窗口收进软件,任务栏/桌面不再单独出现;点「关闭卡片/退出程序」自动还原
- **预览「无图像」修复**:GDI 直接抓窗口客户区(`PrintWindow`,支持 GPU 渲染),不再受窗口遮挡影响;顺带修了 4 字节/像素按 3 字节解析的花屏 bug
- **多开优化**:截图错峰 + 失败退避(最长 4 秒一跳),多开十几个窗口也不烧 CPU
- 自检从 21 项扩充

---

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选时只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |
| Ctrl + B | 打开 Bat 脚本库 |

---

## 完整功能

- **窗口嵌入**:把游戏窗口收进软件内,一键嵌入/弹出,退出自动还原。
- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),GDI 实时抓帧,点击聚焦,双击最大化。
- **多窗口同步 / 单窗口独立**:勾选多个窗口一键同步执行同一套操作;每个窗口又可各自独立配置。
  - **连点器**:多点击点位轮询、可设随机抖动、左右中/双击支持、可视化选点。
  - **键盘宏**:按键序列回放、持续按键、单键循环,任意组合。
  - **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **Steam 状态联动**:挂机自动切状态、停完自动还原(在线 / 离开 / 隐身)。
- **Steam 叠加层控制**:一键关闭 Shift+Tab,写 steam.cfg + 备份还原。
- **Bat 脚本库**:内置 + 用户脚本两路合并,运行不弹黑窗,支持参数。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **定时任务**:定时启停某个动作、每日定时重启游戏。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10+ 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:全局热键、Steam 状态联动、截图帧率、深色/浅色主题、开机自启动、自动检查更新(频率可调)。

---

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\AutoFarmStation\`。
- 窗口嵌入只调用 Windows 自身的 `SetWindowPos`/`SetParent` 窗口接口,不注入、不读写目标进程内存。
- Steam 状态联动只触发 Steam 自己的 `steam://` 协议、只读本地 `localconfig.vdf`:
  **不联网、不读账号密码、不碰 Steam 令牌文件**。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符展示。
- 不上传任何数据,更新检查只读取 GitHub Releases。

---

## 自检

`python -m scripts.selftest` 25 项全过(本次新增 t_update_settings)。

---

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues(已脱敏)。
仓库:[github.com/zlwzk/AutoFarmStation](https://github.com/zlwzk/AutoFarmStation)

---

## 文件说明

- `AutoFarmStation.exe` — 主程序,单文件 exe,Windows 10/11 64 位