# AutoFarmStation v1.5.0 · 全屏聚焦 · 自动唤醒 · 定时任务 · 音量控制 · 卡片自定义

这一版把「多开管理」的四个老大难一次补齐:窗口进来出不去的聚焦、关软件后游戏还在后台跑、
下次打开不想手动一个个开机、以及同时开十几个窗口时吵得受不了的音量。

---

## 本版新增

### 1. 聚焦:游戏界面完整显示(不再贴一半)

以前点「聚焦」只是把窗口提到前台,如果游戏上次是最大化/最小化、或者跨了多显示器,
经常只露出一角。现在聚焦会:

- **按显示器工作区自适应铺满**(避开任务栏,`MonitorFromWindow` + 工作区尺寸);
- **最小化自动还原、最大化保持最大化**;
- `SetForegroundWindow` 失败(后台抢焦点被系统拒绝)时**自动回退**到
  `AttachThreadInput` + `BringWindowToTop` 组合,真的把窗口顶到最前。

设置里可关闭「聚焦时铺满工作区」(设置 → 界面)。

### 2. 退出软件时同步结束游戏进程

关掉多开挂机大师,被追踪的游戏窗口也一起关闭 —— 不用再去任务管理器手动清。

- 默认开启,可在**设置 → 游戏进程**里关掉;
- 「结束进程树」子进程也会一并结束(默认开);
- 确认退出弹框会明确告知「还会一并结束 N 个被追踪的游戏进程」,不会偷偷杀。

### 3. 启动恢复时自动唤醒没开的游戏

上次追踪过、这次没启动的游戏窗口,以前只能匹配到「已经开着的」,没开的就永远丢了。
现在:

- 启动恢复会话时,对**没匹配到的游戏自动重新启动**,并套用上次的连点/宏配置;
- **Steam 游戏会先等 Steam 客户端就绪**,再走 `steam://rungameid/<AppID>` 启动;
- 启动后**自动轮询新窗口句柄并接回追踪列表**(句柄变了也能接上);
- 默认开启(设置 → 游戏进程 → 「启动恢复时自动唤醒未运行的游戏」)。

### 4. 音量控制(设置 → 音量 / 卡片 ♪ 按钮)

| 层级 | 入口 | 说明 |
|------|------|------|
| **整体音量** | 菜单「工具 → 整体音量...」 | 等同任务栏音量条,可静音、可直接拉 0~100% |
| **单窗口音量** | 卡片上的 ♪ 按钮 | 走「音量合成器」那一栏,只影响该游戏进程 |

- 可选「启动时套用保存的整体音量」「新窗口加入时套用默认会话音量」;
- 基于 Windows Core Audio(pycaw),**不装 pycaw 时音量功能自动隐藏/降级**,其它功能不受影响;
- 定时任务里也带了「音量降到 20% / 静音 / 恢复 100%」三个动作。

### 5. 定时任务(菜单「工具 → 定时任务...」,Ctrl+T)

从「只能定时启停连点」升级成完整的调度中心:

- **四种频率**:一次性(指定日期时间)/ 每隔 N 秒·分钟·小时 / 每天 HH:MM / 每周几 HH:MM;
- **动作**:启动全部、停止全部、启动游戏(自动唤醒)、结束游戏进程、开始/停止连点、
  启动键盘宏(指定窗口)、音量降低/静音/恢复、退出本软件;
- 任务列表**持久化**到 `%APPDATA%\AutoFarmStation\schedules.json`,关软件再开还在;
- 侧边「定时」面板显示下次触发倒计时,可暂停/启用/立即执行。

### 6. 卡片上的「停止进程 / 恢复进程」按钮

每张预览卡片新增两个按钮:

- **■ 停止进程**:结束该窗口对应的游戏进程(二次确认,会先记住怎么重新启动它);
- **▶ 恢复进程**:如果游戏已经关了,**一键重新唤醒**(Steam 游戏先开 Steam 再启动游戏),
  起来后自动接回追踪列表。

### 7. 卡片尺寸可调(设置 → 界面)

新增「卡片最小宽度 / 高度」两个数字框(默认 240×190),配合布局列数一起用:
小屏多开想塞更多窗口就调小,想看清画面就调大。改完立即生效,无需重启。

---

## 兼容与升级

- 配置文件向后兼容,新增字段都带默认值,老 `config.json` 直接可用。
- 会话快照新增 `launch` 字段(记录启动信息);老快照读进来依然能匹配窗口,只是不能自动唤醒,
  下次正常运行时会自动补上。
- 自检从 25 项扩到 **27 项**:新增 `audio`(音量读取/降级)、`game_launcher`(VDF 解析 /
  启动信息往返 / 进程状态),并大幅扩充 `scheduler`(四种频率 / 持久化 / 立即执行 / 损坏恢复)。

---

## 全局热键

| 快捷键 | 动作 |
|--------|------|
| F9 | 启动全部(有勾选时只启动勾选的窗口) |
| F10 | 停止全部 |
| Ctrl + Alt + P | 一键急停 |
| Ctrl + T | 定时任务 |
| Ctrl + B | Bat 脚本库 |
| Ctrl + , | 设置 |

---

## 完整功能

- **窗口嵌入**:把游戏窗口收进软件内,一键嵌入/弹出,退出自动还原。
- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),GDI 实时抓帧,点击聚焦(铺满工作区),双击最大化。
- **多窗口同步 / 单窗口独立**:勾选多个窗口一键同步执行同一套操作;每个窗口又可各自独立配置。
  - **连点器**:多点击点位轮询、可设随机抖动、左右中/双击支持、可视化选点。
  - **键盘宏**:按键序列回放、持续按键、单键循环,任意组合。
  - **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **进程管理**:卡片停止/恢复进程,恢复时自动唤醒游戏(Steam 游戏先启动 Steam),退出软件可同步关闭游戏。
- **音量控制**:整体音量 + 单窗口会话音量,可静音、可随任务自动调节。
- **定时任务**:四种频率 + 十种动作,持久化保存,支持暂停/立即执行。
- **Steam 状态联动**:挂机自动切状态、停完自动还原(在线 / 离开 / 隐身)。
- **Steam 叠加层控制**:一键关闭 Shift+Tab,写 steam.cfg + 备份还原。
- **Bat 脚本库**:内置 + 用户脚本两路合并,运行不弹黑窗,支持参数。
- **窗口控制**:置顶、透明度调节,按进程独立配置。
- **进程监控**:进程崩溃检测 + 运行状态实时展示。
- **预设库**:内置 10 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、
  Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、AdVenture Capitalist、Egg, Inc.、通用挂机游戏)。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:界面(卡片尺寸/聚焦铺满)、预览、默认连点参数、游戏进程、音量、Steam、更新与日志。

---

## 隐私

- 100% 本地运行,所有数据保存在 `%APPDATA%\AutoFarmStation\`。
- 窗口嵌入只调用 Windows 自身的 `SetWindowPos`/`SetParent` 窗口接口,不注入、不读写目标进程内存。
- 音量控制只调用 Windows Core Audio 接口,不录屏、不采集音频数据。
- 自动唤醒只调用 `subprocess` / `steam://` 协议,不注入游戏、不改游戏文件。
- Steam 状态联动只触发 Steam 自己的 `steam://` 协议、只读本地 `localconfig.vdf`:
  **不联网、不读账号密码、不碰 Steam 令牌文件**。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符展示。
- 不上传任何数据,更新检查只读取 GitHub Releases。

---

## 自检

`python -m scripts.selftest` **27 项全过**(新增 `audio`、`game_launcher`,扩充 `scheduler`)。

---

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues(已脱敏)。
仓库:[github.com/zlwzk/AutoFarmStation](https://github.com/zlwzk/AutoFarmStation)

---

## 文件说明

- `AutoFarmStation.exe` — 主程序,单文件 exe,Windows 10/11 64 位
