# AutoFarmStation · 多开挂机大师

> 一款专为挂机游戏设计的 Windows 多开自动化工作站。实时预览、连点器、键盘宏、宏录制、多窗口同步执行、Steam 挂机游戏预设,一站搞定。

![banner](https://img.shields.io/badge/version-1.1.0-blue)
![platform](https://img.shields.io/badge/platform-Windows-blue)
![python](https://img.shields.io/badge/python-3.14+-green)
![license](https://img.shields.io/badge/license-MIT-green)

---

## 这是什么

AutoFarmStation(多开挂机大师)是一款面向 **Steam 挂机游戏 / 放置类游戏 / 放置网页游戏** 的 Windows 多开自动化工具。

挂机游戏的核心痛点是:开多个窗口管理不过来,每个窗口都要点、都要按键、都要偶尔维护。AutoFarmStation 把这些事集中到一个界面里:

- **左侧**:所有挂机窗口的实时预览网格,一眼掌控全部状态。
- **右侧**:统一的操作面板,连点器、键盘宏、宏录制、窗口控制、定时任务、预设、统计一应俱全。

不只是 Steam 游戏 — 任意 Windows 进程都可以添加。挂机党、放置玩家、刷子玩家、多开党、模拟器玩家都能用。

## 亮点功能

- **多窗口预览**:左侧自适应网格(1/2/3/4/6 格子),实时刷新,点击聚焦对应窗口,双击最大化。
- **多窗口同步执行 / 单窗口独立执行**(核心):
  - 勾选左侧任意几个窗口 → `📡 应用当前配置` / `▶ 同步启动` / `■ 同步停止`,让它们同时跑同一套连贯操作。
  - 广播只是复制一份初始配置,**每个窗口各存一份**,之后可以分别改成互不相同的操作(窗口 A 点左边、窗口 B 点右边、窗口 C 跑键盘宏,互不干扰)。
  - 也可以完全不勾选,只对某一个窗口单独配置、单独启停。
- **连点器**:多点击点位轮询、可设随机抖动、左右中双击支持、可视化选点、每进程独立配置。
- **键盘宏**:按键序列回放、持续按键、单键循环,任意组合。
- **宏录制/回放**:录制鼠标 + 键盘轨迹,保存为可分享的 JSON,任意窗口回放。
- **窗口控制**:置顶、透明度、位置记忆,按进程独立配置。
- **定时任务**:定时启停某个动作、倒计时关游戏、每日定时重启游戏。
- **进程监控**:进程崩溃检测 + 自动重启、运行状态实时展示。
- **预设库**:内置 10+ 个 Steam 挂机游戏预设(Melvor Idle、Universal Paperclips、Clicker Heroes、Idle Slayer、Mr. Mine、NGU IDLE、Cell to Singularity、AdVenture Capitalist、Egg, Inc. 等),一键应用最佳实践配置。
- **统计**:每进程运行时长、点击/按键次数、累计天数。
- **设置**:全局热键(F9 启动 / F10 停止 / Ctrl+Alt+P 急停)、截图帧率、深色/浅色主题、开机自启动、检查更新。

## 多窗口同步 / 单窗口独立

这是本工具最实用的能力,两种模式可以随时混用:

```
左侧预览网格                          右侧操作面板
┌──────────────┐ ┌──────────────┐
│ ☑ Melvor Idle│ │ ☑ Clicker H. │   ← 勾选 = 加入「同步组」
│   [预览]     │ │   [预览]     │
│ ●连点 00:01:23│ │ ●连点 00:01:23│   两个窗口同步跑同一套点位
└──────────────┘ └──────────────┘
┌──────────────┐
│ ☐ Idle Slayer│   ← 不勾选 = 独立
│   [预览]     │
│ ●连点 00:04:07│   它自己跑一套完全不同的操作
└──────────────┘
```

| 操作 | 效果 |
|------|------|
| 勾选窗口 + `📡 应用当前配置` | 把右侧面板当前这套操作复制给勾选的窗口(不启动) |
| 勾选窗口 + `▶ 同步启动` | 勾选窗口用各自已保存的配置同时开跑 |
| 勾选窗口 + `■ 同步停止` | 一键停掉勾选窗口 |
| 卡片上的「连点」按钮 | 只对**这一个**窗口启停 |
| 右侧面板选中某窗口 + 「启动」 | 只对**这一个**窗口启停 |

## 截图

```
┌─────────────────────────────────────────────────────────────────────┐
│ ➕ 添加窗口  🔄 刷新  | 布局:自适应  预览帧率:1 | ▶ 启动全部  ■ 停止 │
├─────────────────────────────────────────────────────────────────────┤
│ 勾选窗口 → 同步执行 [全选][反选][清空]      已选 2/3  📡应用 ▶同步启动 ■同步停止│
├──────────────────────────────────────┬──────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌────────┐ │ [连点器 键盘宏 录制 控制 预设] │
│ │ ☑Melvor  │ │ ☑Clicker │ │ ☐Idle  │ │                              │
│ │  Idle    │ │ Heroes   │ │ Slayer │ │ 目标:[1234] Melvor Idle      │
│ │  [预览]  │ │  [预览]  │ │ [预览] │ │                              │
│ │ ●连点 00:01:23 │ ●连点 00:01:23 │  │ 间隔: 200 ms                 │
│ └──────────┘ └──────────┘ └────────┘ │ 抖动: 10 %                   │
│                                      │ 点位: (320, 240)             │
│                                      │ [启动][停止][暂停]           │
│                                      │ [📡同步启动][■同步停止]      │
└──────────────────────────────────────┴──────────────────────────────┘
```

## 下载与使用

下载 `AutoFarmStation.exe`(约 46 MB),双击启动,首次启动会创建数据目录。

数据目录(可在设置页查看):
- `%APPDATA%\AutoFarmStation\config.json` — 主配置
- `%APPDATA%\AutoFarmStation\logs\` — 日志(自动脱敏)
- `%APPDATA%\AutoFarmStation\macros\` — 用户录制的宏
- `%APPDATA%\AutoFarmStation\presets\` — 用户自定义预设

## 快速上手

1. 启动你的挂机游戏(Melvor Idle / Clicker Heroes / Idle Slayer / 其他)。
2. 启动 AutoFarmStation。
3. 点击左上角「➕ 添加窗口」,选择游戏窗口(可一次添加多个)。
4. 切换到右侧「预设」面板,选择你的游戏(或「通用挂机游戏」)。
5. 切换回「连点器」,点击「从屏幕选点」,在游戏上点击要连点的位置。
6. **单窗口挂机**:点「启动」,只这一个窗口开始跑。
   **多窗口同步挂机**:在左侧勾选要同步的窗口 → 点「📡 同步启动(勾选窗口)」,它们同时开跑。
7. 想给某个窗口换一套不同的操作?点它的卡片把它设为当前目标,重新配一套点位再启动即可 —— 其它窗口不受影响。
8. 想要一键启停?按 **F9** / **F10**,一键急停按 **Ctrl+Alt+P**(有勾选时 F9 只启动勾选的窗口)。

## 隐私

- **100% 本地运行**,所有数据保存在 `%APPDATA%\AutoFarmStation\`。
- 日志/反馈上传前自动脱敏:Windows 用户名、游戏安装目录、AppData、TEMP、机器名一律替换为占位符。
- UI 不显示 Windows 用户名,所有路径用 `%APPDATA%\...` / `%USERPROFILE%\...` 占位符展示。
- **不上传任何数据**,更新检查只读取 GitHub releases。

## 常见问题

**Q:连点器没生效?**
A:游戏必须以窗口模式运行(非全屏独占)。检查右侧「发送方式」是否选「post 消息」(推荐)。

**Q:可以同时挂多个游戏吗?**
A:可以。添加多个窗口,**每个窗口独立配置点位和宏**。

**Q:几个窗口要做同样的操作,难道要重复配 5 遍?**
A:不用。在左侧勾选那几个窗口,点「📡 应用当前配置」把当前这套操作复制过去,再点「▶ 同步启动」即可。
复制过去之后每个窗口各有独立副本,想给其中一个改成不同的操作也不会影响其它窗口。

**Q:我只想操作其中一个窗口,其它别动?**
A:不勾选任何窗口(或点「清空」),直接点该卡片的「连点」按钮,或在右侧面板选中它后点「启动」——只影响这一个窗口。

**Q:录制宏和连点器有什么区别?**
A:连点器是定点循环点击;宏录制是录制一段完整操作(鼠标轨迹 + 按键)然后在任意窗口回放。两者互补。

**Q:会被 Steam/反作弊检测吗?**
A:本工具只发鼠标键盘输入,不修改游戏进程、不读游戏内存。对大部分挂机游戏(Slither.io、Minecraft 等类似纯挂机游戏)安全。**不保证**对带严格反作弊的游戏(如 VAC)安全 — 请自担风险。

**Q:数据会不会上传?**
A:不会。除非你手动点击「反馈建议」前往 GitHub Issues。

## 自检与开发

```bash
python -m scripts.selftest   # 19 项自检
powershell -ExecutionPolicy Bypass -File scripts\build.ps1   # 打包
```

依赖:
- Python 3.14+
- PySide6 6.6+
- mss 9.0+
- psutil 5.9+
- pynput 1.7+
- pyinstaller 6.0+(仅开发)

## 反馈

应用内「帮助 → 反馈建议」一键前往 GitHub Issues。仓库:[github.com/zlwzk/AutoFarmStation](https://github.com/zlwzk/AutoFarmStation)

---

完全本地运行,无需联网,无广告无追踪,放心挂机。